{{ attach_library('ucb2021_base/ucb-image-gallery') }}
{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}

{% block paragraph %}
	<div{{attributes.addClass(classes)}}>
		<h1>Hello</h1>
		<div class="gallery-div">
			{% for key, item in paragraph.field_image_gallery_photo if key|first != '#' %}
				<a href="{{ file_url(item.entity.field_media_image.entity.fileuri) }}" class="glightbox ucb-gallery-lightbox">
					<img src="{{ file_url(item.entity.field_media_image.entity.fileuri) }}" alt="{{ item.entity.field_media_image.alt }}"/>
				</a>
			{% endfor %}
		</div>
	</div>
{% endblock paragraph %}
